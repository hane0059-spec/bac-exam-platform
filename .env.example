# رابط قاعدة بيانات Neon (PostgreSQL)
# مثال: postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# سرّ توقيع الجلسة — اجعله طويلاً وعشوائياً (32 حرفاً فأكثر)
AUTH_SECRET="ضع-هنا-سلسلة-عشوائية-طويلة-وسرية"

# وضع المنصّة (اختياري): "solo" = نسخة مبسّطة (مدير منصّة + مدرّسون مستقلّون فقط،
# بلا مدارس/مديري مدارس/أولياء أمور/حقول مخصّصة/استيراد مركزي).
# اتركه فارغاً أو احذفه للوضع الكامل المتعدّد المؤسّسات. يُضبَط لكل نشرة.
# NEXT_PUBLIC_PLATFORM_MODE="solo"
