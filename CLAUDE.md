# منصة الاختبارات الإلكترونية — تعليمات المشروع (CLAUDE.md)

> اقرأ هذا الملف قبل أي عمل. التزم به حرفياً. لا تعيد هندسة البنية ولا تغيّر المخطط دون إذن صريح.

## اللغة والواجهة
- ردّ دائماً بالعربية. كل الواجهات عربية، اتجاه RTL، متجاوبة للموبايل والحاسوب.
- مخاطبة حسب الجنس: مدير/مديرة، مدرّس/مدرّسة، طالب/طالبة، «مرحباً بك/بكِ».

## المعمارية (لا تُغيَّر دون إذن صريح)
- تطبيق Next.js 14 واحد (App Router) — واجهة + API. لا Docker، لا Redis، لا Nginx.
- TypeScript. Tailwind بدعم RTL. خطّا Tajawal وReem Kufi.
- PostgreSQL عبر Prisma (Neon مجاناً الآن، مدفوع عند الإطلاق).
- مصادقة JWT عبر `jose` في كوكي httpOnly، والجنس داخل الجلسة.
- تحقّق Zod في كل مسار API.
- تجاهل أي مقترح مناقض (NestJS/Django/Docker/Redis/S3/VPS/Kubernetes) إلا بطلب صريح.

## القرارات المثبّتة (احترمها)
- حقل `gender` على نموذج `User` لكل الأدوار، ومُضمَّن في الجلسة.
- علامة قابلة للتجاوز لكل اختبار عبر `pointsOverride` على `QuizNode`.
- المدرّس المُنشئ + المدير فقط يعدّلان بيانات الطالب وكلمة سرّه.
- نافذة توقيت واحدة لكل اختبار (availableFrom/Until + المحاولات + المهلة).
- تصحيح الإجابة القصيرة: تطبيع عربي (تشكيل/همزات/تاء مربوطة) + مطابقة دقيقة لا جزئية.
- جدول `AppSetting` لإعدادات المدير (الخط/الثيم).
- الدرجة تُخزَّن كنسبة من «الأسئلة الصالحة» لتُعاد الحسبة بأمان عند إلغاء سؤال.
- إعادة حساب الجلسة تجمع الدرجة الفعلية لكل إجابة (`scoreEarned`) فتدعم **الدرجة الجزئية** (مثل 4/5 للمقالي)؛ `computeScore` يقبل `earned` اختيارياً (غيابه = ثنائي `isCorrect×العلامة`).

## الأمان (إلزامي)
- لا تكشف أو تحتفظ ببيانات اعتماد قاعدة البيانات؛ إن ظهرت، نبّه فوراً واطلب إعادة تعيينها.
- تحقّق من الملكية والصلاحية في كل استعلام (المدرّس موارده، الطالب جلسته، ولي الأمر بيانات ابنه).
- لا تُرسل الإجابات الصحيحة للمتصفّح أثناء الأداء.
- فرض المهلة الزمنية على الخادم (وقت البدء من `ExamSession`)، لا على المتصفّح.
- خصوصية القُصّر: لا تُتح بيانات طالب لغير المخوّلين.

## أعراف الكود
- عميل Prisma واحد مشترك (singleton) في `src/lib/prisma.ts` — لا عميل لكل طلب.
- لفّ الكتابات المتعددة في معاملة واحدة (transaction).
- التصحيح يتم على الخادم دائماً.
- اختبارات آلية أولاً للأخطر: دالة التطبيع/التصحيح ودالة إعادة حساب الدرجات.

## طريقة العمل
1. اقترح النطاق بدقّة (ماذا سيُبنى وماذا لن يُبنى الآن) وانتظر الموافقة.
2. ابنِ خطوة واحدة قابلة للاختبار.
3. اسأل قبل أي تغيير في مخطط قاعدة البيانات.
4. بعد كل خطوة وضّح ما يلزم: npm install / db:push / db:seed / إعادة تشغيل.
5. قدّم تعليمات Windows/PowerShell. لا تكسر ما يعمل. أسئلة قليلة ومركّزة.

## بروتوكول الأفكار الطارئة
عند طرح فكرة جديدة أثناء خطوة جارية: لا توقف الخطوة. صنّفها:
(أ) تعديل بسيط آمن → نفّذه وأخبر. (ب) ميزة جديدة → أضِفها للـ Backlog ولا تبدأها قبل إذن.
(ج) تغيير معماري/مخطط → توقّف ونبّه وناقش. واعرض قائمة «أفكار مؤجّلة» في نهاية أي ردّ فيه فكرة مؤجّلة.

## الحالة الحالية (مكتمل ويعمل)
**الحلقة كاملة + إدارة + تعدّد مؤسّسات.** أهمّ ما بُني:

**الأساس:** Next.js 14 (App Router) + TS + Tailwind RTL + خط Cairo (متغيّر `--font-cairo`). مصادقة jose (كوكي httpOnly، الجلسة تحمل sub/role/gender). Prisma/Postgres (Neon). تحقّق Zod في كل مسار. عميل Prisma singleton.

**حلقة الطالب:** `grading.ts` (تطبيع عربي + مطابقة دقيقة + إعادة حساب + ملء الفراغات + مطابقة + حساب عددي) + 42 اختبار Vitest. الأداء (قائمة/بدء/إجابة/تخطّي/نتيجة) بتصحيح على الخادم، مهلة مفروضة من الخادم، **تخطّي مؤقّت** والعودة للمتخطّى آخر الاختبار، تصحيح فوري أو في النهاية حسب الإعداد. الدخول: صفحة بداية بـ**نوافذ حسب الدور** (طلاب/مدرّسون/مدراء/أولياء) — كل نافذة تُلمّح للمُعرّف المناسب، والخلفية موحّدة تحلّ بريد **أو** رمز الطالب **أو** رمز المدرّس **أو** الاسم، مع تحقّق النافذة بعد كلمة السر فقط (لا يكشف وجود الحساب).

**التصحيح اليدوي:** القصيرة تُصحَّح آلياً مبدئياً، والمقالية يدوية؛ كلاهما `needsReview` → الطالب يرى «بانتظار التصحيح» (تُخفى نتيجته النهائية)، والمدرّس يصحّح من صفحة الجلسة (قابل للتعديل دائماً) ويُعاد الحساب.

**أرشيف الطالب + طباعة النتيجة:** بعد إنهاء محاولة، يطبع الطالب نتيجته (`PrintButton` = `window.print`، وعناصر التنقّل `print:hidden` في `DashboardShell`) وينقلها **بطلبه** إلى أرشيفه (`QuizAssignment.studentArchivedAt` عبر `POST /api/student/quizzes/[id]/archive`، حراسة ملكية + اشتراط إنهاء محاولة). الأزرار في صفحة النتيجة (`QuizRunner`/`FileExamRunner`) وعلى بطاقات القائمة. `/student/quizzes` بتبويبَي «النشطة/الأرشيف» (`StudentArchiveToggle`، عرضيّ فقط — لا يؤثّر في الدرجات). للورقي «عرض النتيجة» يعيد فتحها دائماً.

**اعتراض الطالب على التصحيح:** جدول `GradeAppeal` (`AppealStatus{OPEN,ACCEPTED,REJECTED}` + `reason`/`teacherResponse`، مرتبط بالجلسة). الطالب يعترض من صفحة نتيجته (`AppealBox` في `QuizRunner` للعادي و`FileExamRunner` للورقي) **فقط للاختبارات ذات التصحيح اليدوي** (ورقي أو مقالي/قصير/فراغات) **وبعد اعتماد النتيجة** (لا أثناء `needsReview`)، باعتراضٍ مفتوح واحد لكل جلسة، فيُشعَر المدرّس. المدرّس يراجع من `/teacher/appeals` (+ عدّاد على لوحته): يفتح صفحة إعادة التصحيح (الجلسة العادية أو إجابات الورقي) ويردّ **قبول/رفض/إعادة فتح** + ملاحظة، فيُشعَر الطالب ويرى الردّ. الحراسة: الطالب جلسته فقط، والمدرّس مالك الاختبار فقط.

**بنك الأسئلة + المنهج:** أنواع: اختيار/صح-خطأ/قصيرة/**مقالي**/**ترتيب** (ORDER: عناصر تُدخَل بالترتيب الصحيح عبر `QuestionOption.orderNum`؛ تُعرَض مخلوطةً للطالب فيرتّبها بالأسهم، والتصحيح تطابق تسلسل تامّ عبر `gradeOrderAnswer`، وتسلسل الطالب يُخزَّن في `StudentAnswer.textAnswer`، والمراجعة كنصّ مرقّم)/**ملء الفراغات** (FILL_BLANK: نصّ القالب فيه علامات `[[ ]]` لكل فراغ، ولكل فراغ خيارٌ في `QuestionOption` (`orderNum`=رقم الفراغ، `content`=الإجابات المقبولة مفصولةً بـ`|`) **بلا تغيير مخطط**؛ التصحيح آليّ لكل فراغ بـ`gradeFillBlank` (تطبيع عربي + مطابقة دقيقة) بـ**درجة جزئية** بنسبة الفراغات الصحيحة، وإجابات الطالب مصفوفة JSON في `textAnswer`؛ **المراجعة مشروطة**: إن طابقت كل الفراغات الاحتمالات المُدرَجة تُعتمد النتيجة فوراً (`needsReview=false`)، وإن وُجد فراغٌ خارج الاحتمالات (قد يكون مرادفاً صحيحاً غير مُدرَج) يُحال لمراجعة المدرّس؛ الخيارات (الإجابات) لا تُرسَل للمتصفّح أثناء الأداء — الطالب يبني الفراغات من نصّ القالب)/**مطابقة** (MATCHING: أزواج «أيسر↔أيمن» في `QuestionMatchingPair`؛ يرى الطالب اليسرى مرتّبةً واليمنى مخلوطةً (`matchLefts`/`matchRights` في التعقيم، بلا كشف الاقتران) فيختار لكل يسارٍ مطابِقَه، والتصحيح `gradeMatching` بدرجة جزئية ومطابقة دقيقة بعد التطبيع، وإجابة الطالب مصفوفة JSON في `textAnswer`، آليّ فوري بلا مراجعة)/**حساب** (CALCULATION: إجابة عددية، `acceptedAnswers=[القيمة, الهامش±؟]`، التصحيح `gradeCalculation` بـ`parseNumber` (أرقام عربية/فاصلة عربية/فواصل ألفية) ضمن هامش الخطأ، آليّ فوري)/**توسيم رسم** (DIAGRAM_LABEL: المدرّس يرفع صورةً فيها أسهم مرقّمة، ويُدخل إجابات الفراغات المرقّمة بنفس الترتيب (تُخزَّن كـ`QuestionOption` مثل ملء الفراغات)، والصورة `Attachment` نوع `QUESTION_IMAGE` مرتبطة بالسؤال تُبَثّ عبر `/api/attachments/[id]` بفحص ملكية (المدرّس المالك أو طالب مُسنَد/له جلسة)، رفعها من `/api/teacher/questions/[id]/image`؛ الطالب يرى الصورة + فراغات مرقّمة ويملؤها، والتصحيح **عين منطق ملء الفراغات** (`gradeFillBlank`: آليّ فوريّ عند المطابقة الكاملة، ومراجعة المدرّس عند أيّ خطأ، بدرجة جزئية)؛ التعقيم يرسل `blankCount`+`imageId` بلا كشف الإجابات). تشجير: المادة ← **وحدة (`Unit`)** ← فصل (`Chapter`) ← درس (`Concept`). المدرّس يبني المنهج (`/teacher/curriculum`) ويفلتر البنك بالتشجير + ترقيم صفحات. قفل بنية السؤال المُستخدَم.

**تكوين الاختبار والإسناد:** باني خطّي (اختيار/ترتيب/`pointsOverride`/إعدادات/نافذة توقيت/نشر) مع قفل المنشور/المُستخدَم. **بنك أسئلة الباني مفلتَر** (بالفصل + بحث نصّي) لئلّا يُعرَض كل أسئلة المادة دفعةً. لوحة تصحيح المدرّس تشمل كل الأنواع اليدوية (قصيرة/مقالي/ملء فراغات/**توسيم رسم**). محرّر إجابات الفراغات فيه زرّ «+ مرادف |» لإدراج فاصل المرادفات. **دورة حياة الاختبار** (`QuizStatus`: DRAFT/PUBLISHED/ARCHIVED، عبر `publish` route و`DELETE`): **إلغاء النشر مسموح بعد أداء الطلاب** (PUBLISHED→DRAFT، يُمنع فقط أثناء محاولة جارية الآن؛ النتائج تبقى محفوظة والاختبار يختفي عن الطلاب)؛ **الحذف يؤرشف دائماً** (→ARCHIVED، قابل للاستعادة، مخفيٌّ عن الطلاب)؛ **استعادة** من الأرشيف (`action:"restore"`→DRAFT)؛ **حذف نهائي** من الأرشيف فقط (`DELETE ?permanent=1`): **يُبقي سجلّ الدرجة** (من أدّى وكم أخذ) ويحذف المحتوى — يُمحى أسئلة الاختبار (`QuizNode`/`QuizEdge`) والمرفقات (ملف الاختبار + أوراق الإجابات + تعليقاتها) وتفاصيل الإجابات (`StudentAnswer`) والإسنادات، وتبقى **قشرة الاختبار** (العنوان/المادة) و**الجلسات** (`ExamSession` بدرجاتها) مع علامة `settings.purged=true` (شارة «محتوى محذوف» + بانر، وتُعطَّل الاستعادة/النشر). مفيدٌ عند خطأ فادح أو تسرّب. وإن **لم يؤدِّه أحد** (لا جلسات) فيُحذف بالكامل (cascade). يسري على العادي (`QuizBuilder`) والورقي (`FileExamManager`) بمسارات مشتركة. **أرشيف المدرّس التلقائي** (`/teacher/quizzes` بتبويبَي «النشطة/الأرشيف»): الاختبار يُصنَّف **مكتملاً** وينتقل لتبويب الأرشيف تلقائياً (مُشتقّ بـ`computeDoneQuizIds`، بلا تغيير حالة فلا يُحجَب عن الطلاب) متى أُسنِد لطالب واحد فأكثر، وأكمل **كلُّ** المُسنَد إليهم محاولةً، ولا إجابة بانتظار مراجعة ولا جلسة ورقية `needsGrading` ولا اعتراض مفتوح؛ والأرشيف يضمّ أيضاً المؤرشف (المحذوف). دقيقٌ دائماً (يعود للنشط تلقائياً عند إسناد طالب جديد أو فتح اعتراض). **خلط النزاهة** (`settings.shuffle`): عند تفعيله يُخلَط ترتيب الأسئلة والخيارات **لكل محاولة** ببذرة ثابتة (طالب+اختبار+رقم المحاولة، تستقرّ عند الاستئناف) — عرضيٌّ فقط، والتصحيح بمعرّفات الخيارات لا بترتيبها. الإسناد: فردي/متعدّد بالاختيار/للكل/**بالرمز** (لصق رموز طلاب المؤسّسة)/**رمز الاختبار** (`accessCode` تسلسلي، انضمام الطالب به مع مفتاح تفعيل `allowCodeJoin`). منح محاولة إضافية/تصفير لكل طالب (`extraAttempts`).

**المتابعة:** نتائج المدرّس لكل اختبار + مراجعة جلسة كل طالب (`getSessionReview` مشترك) + **تصدير النتائج Excel** (`/api/teacher/quizzes/[id]/results/export`، RTL، بعزل الملكية).

**بحث المدرّس في طلابه:** صندوق بحث في `/teacher/students` (بالاسم/الرمز/البريد/الهاتف) ضمن طلابه (إنشاءً أو تسجيلاً) — يعيد استخدام `UserSearchBox` بـ`basePath`.

**صفحة الطالب لدى المدرّس + إسناد من جهة الطالب:** كل بطاقة طالب في `/teacher/students` تفتح `/teacher/students/[id]` (متاحة لأي مدرّس على طلابه — إنشاءً أو تسجيلاً، **بلا اشتراط `canManageStudents`** لأنّ الإسناد حرّ). تعرض معلومات الطالب و**لوحة إسناد مقلوبة** (`StudentAssignQuiz`): يختار المدرّس من اختباراته **المنشورة في مواد الطالب المسجّلة معه** + موعد اختياري → تعيد استخدام `POST /api/teacher/quizzes/[id]/assignments` بـ`{studentIds:[id]}` (بكل حراساته: ملكية + نشر + تسجيل الطالب في مادة الاختبار)، وتعرض الإسنادات الحالية مع إلغائها (DELETE، يُمنع أثناء جلسة جارية). رابط «تعديل البيانات» يظهر عند `canManageStudents` فقط.

**إدارة المدرّس لطلابه:** **بإذن المدير فقط** (`TeacherProfile.canManageStudents`، مغلق افتراضياً، مفروض على الخادم في كل عمليات الإنشاء/التعديل/كلمة السر/التسجيل + إخفاء الأزرار) — أمّا **إسناد الاختبارات فحرّ للمدرّس دائماً**. عند الإذن: إنشاء/تعديل/كلمة سرّ، رمز طالب تسلسلي تلقائي، حقول (أب إجباري/أم/عنوان/هاتف الطالب)، تسجيل في المواد.

**المدرّس المستقلّ (مدير ومدرّس):** المدير العام ينشئ من `/admin/users/new` **مدرّساً مستقلّاً** يدير مؤسّسته بنفسه: تُنشأ له **مؤسّسة خاصّة تلقائياً** (`School` باسمه، نوع «مدرّس مستقل») ويُربط بها لعزل طلابه، ويُفرَض له `TeacherProfile.{isIndependent=true, canManageStudents=true}` + **حدّ طلاب** `studentLimit` (أساس الاشتراك؛ الفوترة مؤجّلة). يعمل من لوحة المدرّس المعتادة (يسجّل طلابه ويبني منهجه واختباراته). **فرض الحدّ على الخادم** في `POST /api/teacher/students` (عدّ طلابه `createdById`، يُرفَض عند البلوغ)، مع عرض «العدد/الحدّ» وتعطيل «+ طالب جديد» في `/teacher/students`. المدير العام يعدّل الحدّ لاحقاً من صفحة تعديل المدرّس.

**إدارة المدير لطلاب مؤسّسته:** المدير يدير **كل** طلاب مؤسّسته: إنشاء (`/admin/students/new`) + **تعديل/إعادة كلمة سرّ** (`/admin/students/[id]/edit`، رابط من شجرة المستخدمين) بعزل المؤسّسة (`StudentForm` بـ`updateEndpoint`، و`PasswordResetForm` العامّ).

**تعدّد المؤسّسات:** `School` + `User.schoolId`. **مدير عام للمنصّة** (schoolId=null, isSuperAdmin) ينشئ المدارس/المعاهد ومديريها والمواد/الصفوف والحقول المخصّصة. **مدير مدرسة** (admin بـ schoolId) يدير مستخدمي مؤسّسته فقط (عزل في كل الاستعلامات). وراثة `schoolId` عند الإنشاء.

**لوحة المدير:** المستخدمون في عرض **شجري** (تبويب الطلاب: مؤسّسة←صفّ←طلاب؛ تبويب المدرّسين/المدراء: مؤسّسة←أعضاء للمدير العام، أو حسب الدور لمدير المدرسة — `UsersTree`)، **بحث المستخدمين** (بالاسم/الرمز/البريد/الهاتف، نتائج مسطّحة برابط تحرير لكل دور — للمدير العام عبر الجميع، ولمدير المدرسة ضمن مؤسّسته)، إنشاء مدرّس/مدير/طالب، المدارس، **المواد والصفوف متداخلة** (الصفّ←مواده مع تحرير/حذف لكل عنصر — `AcademicsTree`، للمدير العام)، **الحقول المخصّصة** (`CustomFieldDef` + `User.customData`: المدير العام يعرّف حقولاً إجبارية/اختيارية لكل جمهور)، استيراد طلاب خارجيين من **Excel فقط** (exceljs) + تصدير بيانات الدخول xlsx.

**لوحة المدير العام (الإشراف عبر المؤسّسات):** صفحة نظرة عامة (`/admin/overview`: إحصاءات المنصّة + جدول لكل مؤسّسة)، تصفّح **اختبارات وأسئلة المؤسّسات** (`/admin/quizzes` و`/admin/questions`: قراءة فقط، فلترة بالمؤسّسة/المادة/النوع أو الحالة + ترقيم)، **التخزين والمرفقات** (`/admin/storage`: حجم المرفقات الكلّي + حسب النوع + حسب المؤسّسة)، و**إدارة الاحتفاظ** (`/admin/retention`: جدول المستخدمين مرتَّباً حسب حجم مرفقاتهم مع تبويب «المعطّلون فقط» — زرّ «تفريغ المرفقات» لكل حساب معطَّل فقط، عملية لا رجعة فيها، تحتاج تأكيدَين صريحَين، تُعيد البايتات المُحرَّرة). النسبة للمؤسّسة عبر `creator.schoolId`/`student.schoolId`.

**ولي الأمر:** دور `PARENT` + جدول ربط `ParentLink` (متعدّد-لمتعدّد: ولي ← عدّة أبناء، وطالب ← عدّة أولياء). **المدير** ينشئ الوليّ ويربطه بأبنائه بلصق رموز الطلاب (بعزل المؤسّسة) من `/admin/parents` (قائمة/إنشاء/إدارة روابط). **لوحة الوليّ** `/parent`: أبناؤه ونتائج كلّ ابن (قراءة فقط) عبر `SessionReviewView` المشترك — مع **فحص ملكية صارم** (`parentOwnsStudent`) لكل وصول لبيانات طالب، وإخفاء النتيجة ما دامت بانتظار التصحيح. الدخول الموحّد يعمل للوليّ (بريد/اسم).

**الاختبار الورقي/المرفوع (تخزين داخل Postgres):** **خاصّية اختيارية** يفعّلها المدير لكل مدرّس عند الطلب (`TeacherProfile.canFileExams`، مغلقة افتراضياً، مفروضة على الخادم في الإنشاء + إخفاء الزرّ) — لتقليل التخزين. نوع اختبار موازٍ (`Quiz.isFileBased`) بلا أسئلة/عُقد: المدرّس ينشئه ويرفع ورقته (صورة/PDF) من `/teacher/file-exams`، والطالب يرفع صور إجابته من صفحة الأداء ثم يُرسلها (`needsGrading`)، والمدرّس يصحّح يدوياً (درجة كلّية + ملاحظة) من «الإجابات والتصحيح»، ويراها الطالب ووليّ أمره. الملفات في جدول `Attachment` (bytea) خلف طبقة تجريد `src/lib/attachments.ts`، وتُبَثّ عبر `/api/attachments/[id]` **بفحص ملكية صارم فقط** (لا روابط عامّة). حدود: صورة/PDF ≤3MB، 12 صفحة. **مهلة زمنية اختيارية مفروضة على الخادم** (من `startedAt`): عند انتهائها تُرسَل الجلسة للتصحيح إن وُجدت صور وإلا `TIMED_OUT` (`finalizeFileSessionIfExpired` تُستدعى في البدء/الرفع/الإرسال وعند فتح الصفحة)، مع عدّاد تنازلي في الواجهة. الدرجة تُخزَّن في `ExamSession.{totalScore,maxPossibleScore,percentage}` + `teacherFeedback`. الرفع عبر `ImageUploadField` المشترك: **ضغط الصورة على المتصفّح** (`imageCompress.ts`: تصغير + JPEG) ثم **معاينة قبل التأكيد** (يرى الرافِع الوضوح والأبعاد والحجم، فيرفع أو يحذف ويختار غيرها).

**الإشعارات (داخل التطبيق):** جدول `Notification` + جرس بعدّاد غير المقروء في رأس كل لوحة + صفحة `/notifications` (تُعلَّم مقروءةً عند الفتح، وتُعرَض عموميّاً بالرسالة + `linkUrl` بلا قائمة أنواع). يُنشَأ إشعار للطالب عند **أوّل تصحيح** لورقته (لا يتكرّر عند التعديل). و**يُشعَر المدرّس مالك الاختبار عند تسليم الطالب**: في الاختبار العادي عبر `finalizeSession` (مرّةً عند الانتقال من «قيد الأداء»، نوع `exam_needs_grading` إن وُجدت إجابة بانتظار المراجعة وإلا `exam_submitted` بالنسبة، يربط لـ`/teacher/sessions/[id]`)، وفي الورقي عبر `notifyFileExamSubmitted` (من مسار الإرسال وعند انتهاء المهلة بوجود صور، يربط لـ`/teacher/file-exams/[id]/submissions`). فشل الإشعار لا يكسر التسليم.

**تعليقات المدرّس على الصورة:** جدول `Annotation` (إحداثيات نسبية x,y على صورة الإجابة) — المدرّس ينقر الصورة فيضع دبّوساً بتعليق من «الإجابات والتصحيح» (`ImageAnnotator` editable)، ويراها الطالب ووليّ أمره دبابيس فوق الصورة (قراءة فقط). التعليقات على الصور لا PDF.

**أدلّة الاستخدام:** زرّ «كيف أستخدم صفحتي؟» في رأس كل لوحة يفتح `/guide/[role]` (تبويب جديد) — دليل مخصّص لكل دور (طالب/مدرّس/مدير/وليّ)، محتواه في `src/lib/guide.ts`.

**كلمات السر:** مفتاح **إظهار/إخفاء** (`PasswordInput`) على حقول الدخول وإعادة التعيين (لا تُكشف كلمات السر المخزَّنة — مُجزَّأة bcrypt باتجاه واحد، يُظهَر ما يُكتَب فقط). **تغيير ذاتي**: كل مستخدم يغيّر كلمته من `/account` (رابط «حسابي» في كل لوحة) بإدخال الحالية + الجديدة (`/api/account/password` يتحقّق من الحالية).

**تسجيل الطلاب في المواد:** المدرّس يسجّل طلابه في موادّه؛ و**المدير** يسجّل/يلغي تسجيل أي طالب في مؤسّسته مع أيّ مدرّس+مادة من صفحة تعديل الطالب (`AdminEnrollmentManager` + `/api/admin/students/[id]/enrollments`، بعزل المؤسّسة).

**إتاحة وصول:** متحكّم تكبير النصّ (يُحفظ على الجهاز)، زرّ «الرئيسية» في كل صفحة. تواريخ بحقل أرقام خاصّ (`DateTimeField`) + عرض `formatDateTime` داخل `‹bdi›`.

**شارات الإنجاز للطالب:** `src/lib/badges.ts` — دالة `computeBadges(BadgeInputs): Badge[]` نقية (بلا قاعدة بيانات) تشتقّ 8 شارات من إحصاءات الجلسات المنتهية: الخطوة الأولى (1 اختبار) / المثابر (5) / المجتهد (10) / العلامة العالية (≥90%) / المثالي (100%) / سيّد المفهوم (80% في مفهوم واحد) / 5 مفاهيم / سيّد المادة. `BadgeGrid` يعرض المُكتسَبة (ذهبية) والمقفولة (رمادية) بعدّادات تقدّم في `/student/progress`.

**استيراد بنك الأسئلة الخارجي + البنك العام:**
- `src/lib/questionImport.ts` — دالة `normalizeBankJson(raw)` نقية تحوِّل JSON خارجياً (أنواع: `multiple_choice/true_false/ORDER/MATCH/CONCEPT_MAP/DIAGRAM_LABEL/ESSAY` + مرادفاتها) إلى `NormalizedQuestion[]` مع تجميع الأخطاء لكل سؤال. قاعدة التحويل: `MCQ`→`MULTIPLE_CHOICE`، `TF`→`TRUE_FALSE`، `ORDER`→`ORDER`، `MATCH`→`MATCHING`، `CONCEPT_MAP`→`FILL_BLANK`، `LABEL/DIAGRAM_LABEL`→`DIAGRAM_LABEL`، والباقي→`ESSAY`. `DIAGRAM_LABEL` يُحوَّل لخيارات مثل `FILL_BLANK` ويُضاف `image_description` للنصّ مع تحذير «الصورة لاحقاً». 80+ اختبار Vitest.
- `src/lib/questionImportServer.ts` — `prepareImport(file, targets)` مشترك (teacher + admin): يُطبَّع + يُتحقَّق Zod + ملخّص.
- `src/lib/questionCreate.ts` — `buildQuestionCreateData(d, {creatorId, isPublic?})` مشترك لبناء payload الإنشاء في Prisma.
- `POST /api/teacher/questions/import` — المدرّس يستورد لبنكه الخاصّ (`isPublic:false`).
- `POST /api/admin/questions/import` — المدير العام فقط يستورد للبنك العام (`isPublic:true`).
- `<QuestionImporter>` — مكوِّن قابل للإعادة (`endpoint`/`bankPath`/`bankLabel` كـprops): معاينة قبل التأكيد + ملخّص (مقبولة/مرفوضة/إجمالي العلامات).
- `/admin/questions/import` — صفحة استيراد للمدير العام (كل المواد).
- **البنك العام** — `Question.isPublic`: المدرّس يتصفّح أسئلة البنك العام في موادّه (`/teacher/questions/public`، فلترة بالمادة/النوع/النصّ + ترقيم) وينسخ السؤال لبنكه الخاصّ (`POST /api/teacher/questions/[id]/copy`، يتحقّق من `isPublic` وتدريس المادة، ينسخ بـ`isPublic:false`). زرّ `<CopyPublicQuestionButton>` بحالات idle→loading→done/error.
- **BANK/IMPORT_SPEC.md** — مواصفة تقنية كاملة لصيغة JSON الخارجية (للمطوّرين وأنظمة الاستيراد الآلي).
- **BANK/TEACHER_PROMPT.md** — نصّ جاهز للنسخ يعطيه المدرّس لذكاء اصطناعي محلّي (ChatGPT/Claude) لتوليد JSON متوافق مع المنصّة دون تعديل.

**هوية المنصّة وصفحة الدخول (تحكّم المدير العام):** العلامة «إتقان» (شعار حلزون دنا ذهبي افتراضي في `BrandLogo`، اسمٌ بخطّ Reem Kufi كعلامة كلمية، حكمة بخطّ Amiri أسفل الدخول). يضبط المدير العام **كلّ ما يظهر للزائر** من `/admin/settings` (`BrandingForm`): الاسم + **خطّ الاسم** (يُختار من فهرس خطوط موسّع) + الشعار النصّي + **رفع صورة شعار مخصّصة** (PNG/JPEG/WebP ≤1MB، تُبَثّ عبر `/api/branding/logo` العامّ، وبدونها الرمز الافتراضي)، نصّ الحكمة وإظهارها و**حجمها** (6 درجات sm→3xl عبر `QUOTE_SIZE_CLASS`)، **ملاحظة/إعلان عامّ** (بانر في الدخول وكل اللوحات، معلومة/تحذير)، **وضع الصيانة** الذي **يوقف تسجيل الدخول فعلياً** لكل الأدوار عدا المدير العام (مفروض في `/api/auth/login` برمز 503)، التواصل (بريد/هاتف) و«عن المنصّة» في تذييل الدخول، و**إظهار/إخفاء نوافذ الدخول حسب الدور + تخطيطها** (شبكة/قائمة). صفحة الدخول حيويّة (توهّج خلفيّ، أيقونات أدوار ملوّنة، فاصل زخرفيّ). **بلا تغيير مخطط**: كل الإعدادات في `AppSetting` (مفتاح JSON `branding`، والشعار في `branding_logo`). الأنواع/الثوابت الآمنة للعميل في `src/lib/brandingShared.ts` منفصلةً عن دوال القاعدة في `src/lib/branding.ts` (لئلّا تُسحَب prisma/cache لحزمة العميل). عنوان المستند يُشتقّ من الاسم (`generateMetadata`). **فهرس الخطوط** في `src/lib/fonts.ts` (آمن للعميل): ~18 خطّاً — خطوط ويب مضمّنة (Cairo/Tajawal/Reem Kufi/Amiri عبر next/font، تظهر للجميع) + خطوط نظام عربية كثيرة (Traditional/Simplified/Sakkal Majalla/Tahoma/Segoe UI/Geeza Pro/Noto… مع ارتداد ويب آمن إن لم تتوفّر على جهاز الزائر). يضبط المدير العام خطّ المنصّة كاملاً (`--font-app`) من `SettingsForm`، وخطّ اسم المنصّة وحده من `BrandingForm` (`branding.nameFont`، أو "app" لوراثة خطّ المنصّة، يُحلّ إلى CSS على الخادم في `login/page`).

**دورة حياة حساب المدرّس (مغادرة آمنة):**
- `POST /api/admin/users/[id]/set-active` — تفعيل/تعطيل مدرّس بدون أو مع cascade لطلابه، في transaction.
- `POST /api/admin/users/[id]/offboard` — **المغادرة الذكية** (`teacherOffboard.ts` في transaction واحدة): (1) تعطيل المدرّس، (2) تعطيل كل تسجيلاته مع الطلاب، (3) طلابه الذين خُلِق حسابهم بـ`createdById` ولا تسجيلات فعّالة أخرى → يُعطَّلون؛ ذوو تسجيلات أخرى → يبقون. (4) حذف مرفقات المدرّس + الطلاب الحصريّين. يُعيد: `{deactivatedStudents, keptStudents, deletedAttachments, freedBytes}`.
- `GET /api/admin/users/[id]/export` — تصدير حزمة المدرّس الكاملة JSON (اختبارات/أسئلة/طلاب/جلسات/مرفقات بـbase64، `TEACHER_EXPORT_FORMAT=bac-teacher-export v1`) للتنزيل بترويسة `Content-Disposition`.
- `POST /api/teacher/account/deletion-request` — المدرّس يطلب حذف حسابه من `/account`: ينشئ إشعاراً لكل المدراء (schoolId + superadmin)، مع تكرار-مانع (إن وُجد إشعار مفتوح بنفس الرابط يُعيد `{alreadyRequested:true}`).
- `<TeacherLifecyclePanel>` في صفحة تعديل المدرّس (`/admin/users/[id]/edit`): رابط تصدير + مفتاح تعطيل/تفعيل + قسم «المغادرة الذكية» بتأكيد مزدوج يعرض النتيجة تفصيلاً.
- `<RequestAccountDeletion>` في `/account` للمدرّس: تأكيد مزدوج + يوضّح أنّه طلب للمدير لا حذف فوري.

**إضافات المخطط بعد الأساس:** `Unit`، `School`، `CustomFieldDef`، **`ParentLink`**، **`AppSetting`**، **`Attachment`** (+`AttachmentKind` بقيمة `QUESTION_IMAGE` و`Attachment.questionId`، `Quiz.isFileBased`، `ExamSession.{needsGrading,teacherFeedback}`)، **`Notification`**، **`Annotation`**، **`QuestionReport`** (+`QuestionReportStatus`)، **`GradeAppeal`** (+`AppealStatus`، علاقتا `ExamSession.appeals` و`User.gradeAppeals`) (+ قيمة `PARENT` في `Role` وعلاقتا `User.{parentLinks, studentParents}`)؛ وحقول: `User.{isSuperAdmin, schoolId, createdById, customData}`, `Chapter.unitId`, `Quiz.{accessCode, allowCodeJoin}`, `QuizAssignment.{extraAttempts, studentArchivedAt}`, `StudentProfile.{fatherName, motherName, address, isExternal}`, `StudentAnswer.needsReview`, `TeacherProfile.{isIndependent, studentLimit}`, `User.email` صار اختيارياً، `Question.isPublic` (للبنك العام).

> البذرة الحالية (`prisma/seed.ts`) **تصفّر كل شيء** ثم تبني **سيناريو اختبار شاملاً**: المدير العام + صفّ «بكالوريا علمي» + 9 مواد مُشجَّرة، ومؤسّسة «ثانوية النور» بمدير مدرسة و**12 مدرّساً** (مدرّس لكل مادة + مدرّس ثانٍ للمواد الثلاث BIO/PHYS/CHEM) و**طالبَين** مسجّلَين في المواد الثلاث، مع بيانات تُمرّن كل الميزات: بنك أسئلة بكل الأنواع (+ملء الفراغات)، واختبارات في كل الحالات (تلقائي مكتمل→أرشيف المدرّس، تصحيح يدويّ+اعتراض مفتوح، مسودّة، مؤرشف، محذوف المحتوى مع بقاء الدرجة، ورقي مُصحَّح بتعليق صورة)، وأرشفة طالب وإشعارات. الحسابات: `admin@example.com`/`Admin@123` (عام)، `manager@nour.edu`/`Admin@123` (مدير مدرسة)، `t.bio@nour.edu`/`Teacher@123` (صلاحيات ملفّات+إدارة طلاب)، `student1@nour.edu`/`student2@nour.edu`/`Student@123`. استخدم Git دائماً.

## خارطة الطريق (التالي)
- ~~تشجير لوحة المدير~~ ✅ تمّ: تبويب الطلاب/المدرّسين شجرياً + صفحة «الصفّ ← مواده» المتداخلة.
- ~~تحميل كسول لشجرة المستخدمين~~ ✅ تمّ: البنية والأعداد تُرسَل فوراً، وعناصر كل صفّ/مؤسّسة تُجلَب عند الفتح عبر `GET /api/admin/users/tree-leaves` (`TreeLazy` descriptor + `Branch` component).
- ~~لوحة المدير العام: إشراف على البنك العام والاختبارات عبر المؤسّسات~~ ✅ تمّ (نظرة عامة + تصفّح البنك والاختبارات، قراءة فقط).
- ~~دور ولي الأمر~~ ✅ تمّ (دور `PARENT` + `ParentLink` + لوحة الوليّ، قراءة فقط). لاحقاً: ربط الوليّ بأبنائه من المدرّس أيضاً.
- ~~سياسة الاحتفاظ ومسح المرفقات~~ ✅ تمّ: `/admin/retention` + `purge-attachments` API.
- ~~دورة حياة المدرّس (تعطيل/تصدير/مغادرة/طلب حذف)~~ ✅ تمّ.
- ~~استيراد بنك الأسئلة الخارجي + البنك العام~~ ✅ تمّ.
- ~~شارات الإنجاز للطالب~~ ✅ تمّ.
- **إرسال بيانات الدخول** بالبريد ثم SMS (خدمات خارجية — يلزم مزوّد ومفاتيح). **مؤجّل**: لا مزوّد متاح حالياً.
- **مراجعة أمنية قبل الإطلاق** — audit كل مسارات API (ملكية/صلاحية/عزل المؤسّسة/منع تسرّب الإجابات/المهل الزمنية). **التالي**.
- بنود Backlog أدناه عند الإذن.

## Backlog (أفكار مؤجّلة)
> مصدرها مواصفات «بنك الأسئلة الشامل» (علم الأحياء — ثالث ثانوي 2026). لا تُبدأ قبل إذن صريح.
> ملاحظة: المخطط الحالي يغطّي معظم أنواع الأسئلة عبر `QuestionType` + `tags` + `mediaUrls`؛ «أنماط البكالوريا» تُمثَّل وسوماً لا أنواعاً جديدة.

1. ~~نوع سؤال ORDER/SEQUENCE~~ ✅ تمّ (قيمة `ORDER` في enum + إعادة استخدام `QuestionOption.orderNum` للترتيب الصحيح + خلط العرض + `gradeOrderAnswer` + مراجعة نصّية).
2. **مولّد نماذج اختبار عشوائية** — توليد 5–7 نماذج غير متطابقة من بنك حسب نطاق صفحات/درس. ميزة مستقلة عن نهج شجرة `QuizNode/QuizEdge`؛ لا تُخلط بها.
3. **ربط الموقع بـ Claude API** لتوليد أسئلة عند الطلب (تغيير معماري — يُناقَش).
4. **زر «الإبلاغ عن خطأ في السؤال»** ✅ تمّ: الطالب يبلّغ أثناء الأداء (`QuizRunner`) → جدول `QuestionReport` + إشعار المدرّس، ومسار مراجعة `/teacher/reports` (معالجة/تجاهل/إعادة فتح + ملاحظة، بعدّاد على لوحة المدرّس). **+ إلغاء السؤال** (`Question.isCancelled`) من صفحة البلاغات: يُلغى السؤال (يُستبعد من حساب الدرجة بسطاً ومقاماً عبر `computeScore`) **ويُعاد حساب كل الجلسات المتأثّرة** تلقائياً + إشعار الطلاب، مع إمكانية التراجع.
4b. ~~نوع سؤال «ملء الفراغات» (FILL_BLANK)~~ ✅ تمّ (متعدّد الفراغات بعلامات `[[ ]]` + درجة جزئية لكل فراغ عبر `gradeFillBlank` + خاضع لمراجعة المدرّس + بلا تغيير مخطط بإعادة استخدام `QuestionOption`).
5. **بطاقات مراجعة (Flashcards)** اعتماداً على `tags`.
6. **وسوم أنماط البكالوريا الموحّدة** (`bac_negative`, `bac_trap`, `application`, …) معياراً ثابتاً على الأسئلة.
7. ~~طباعة/تصدير الاختبار وسلّم التصحيح~~ ✅ تمّ للمدرّس: `/teacher/quizzes/[id]/print` (تبديل ورقة الأسئلة/سلّم التصحيح + طباعة المتصفّح، `print:` وأنماط `@media print`). لاحقاً: للطالب بعد الانتهاء.
8. **تبديل الخط** ✅ تمّ: `/admin/settings` (المدير العام) يختار خطّ المنصّة عبر `AppSetting`؛ المتغيّر المستهلَك `--font-app` يُضبَط ديناميكياً في `layout` من `FONT_CSS`. الخيارات: Cairo/Tajawal/Reem Kufi + **Traditional Arabic** و**Times New Roman** (خطّان نظاميّان بمكافئ ويب يُحمَّل للجميع: Amiri للعربية، Tinos للّاتينية). **الثيم/الوضع الليلي** ✅: ألوان محايدة (ink/parchment/surface/line) متغيّرات CSS بقنوات RGB، صنف `.dark` على `<html>`، مفتاح `ThemeToggle` يُحفظ على الجهاز (`bac-theme`) ويُطبَّق قبل الرسم في `layout` (بلا وميض)، والطباعة تُجبَر فاتحةً. العلامة (primary/gold) ثابتة.
9. **إتاحة وصول إضافية**: وضع «تباين عالٍ» للرؤية الضعيفة، وربما قراءة صوتية للأسئلة. (تمّ بالفعل: متحكّم تكبير حجم النصّ لكامل الموقع، يُحفظ على الجهاز.)

## أوامر التشغيل
- `npm install` ثم `.env` (DATABASE_URL لـ Neon + AUTH_SECRET طويل).
- `npm run db:push` ← `npm run db:seed` ← `npm run dev` → http://localhost:3000
- **بعد أي تغيير مخطط:** أوقف `dev` ثم `npx prisma generate` ثم `npm run dev` (الخادم يقفل محرّك Prisma).
- الحساب الوحيد بعد البذرة: **المدير العام** `admin@example.com` / `Admin@123` (بقية الحسابات تُنشأ من اللوحة).

## ملاحظات تشغيلية للمساعد
- العمل في worktree؛ بعد كل خطوة: `tsc --noEmit` + `npm run build`، ثم commit في الفرع و`git -C <main> merge --ff-only <branch>`، وتغييرات المخطط تُطبَّق بـ `prisma db push` على Neon.
- تغييرات الكود فقط: يكفي تحديث المتصفّح (الخادم يعيد الترجمة). تغييرات المخطط: تتطلّب `prisma generate` + إعادة تشغيل.
